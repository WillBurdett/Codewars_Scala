package Workbook

object ListsAndArrays extends App {
  // Lists are immutable (can't reassign values, and also represent a LinkedList
  val testList: List[Int] = List(1, 2, 3);
  // to find item an
  println(testList(1))
}
